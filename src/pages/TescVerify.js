import React from 'react';
import { Input, Label, Form, TextArea, Checkbox, Button } from 'semantic-ui-react';


const TeSCVerify = () => {

    return (
        <Form>
            <Form.Input label='TeSC Address' placeholder='www.mysite.com' width={6} />
        </Form>

    );
};

export default TeSCVerify;